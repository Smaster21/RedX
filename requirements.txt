# Core (required)
rich>=13.0.0
requests>=2.28.0
prompt_toolkit>=3.0.0

# Optional (for better web scraping in web_search tool)
beautifulsoup4>=4.12.0

# Optional (for MCP server support)
# Install only if you use /mcp commands
